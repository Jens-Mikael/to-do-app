import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: '#6302ef',
    },
    secondary: {
      main: '#ff430f',
    },
    background: {
      default: '#6302ef',
      paper: '#111120',
    },
  },
});

